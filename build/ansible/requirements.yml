---
# Install with:  ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general   # ufw module
    version: ">=8.0.0"
  - name: ansible.posix       # acl module
    version: ">=1.5.0"
